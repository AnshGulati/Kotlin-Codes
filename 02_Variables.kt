fun main() {
    // var can be changed whereas val can't.

    var score = 5;
    println(score);

    score = 10;
    println(score);

    val message = "I cannot be changed!";
//    message = "Showing Error";
}